exports = module.exports = function () {
    var _this = this;

    //returns checkpoints
    this.getCheckpoints = function () {
    };

    //Returns boolean
    this.achievedCheckpoint = function (checkpoint, cube) {

    };

    //Returns the next move
    this.computeNextMove = function (cube, currentCheckpoint) {

    };
};